import React from "react";
import SignInClient from "./client";

export const metadata = {
  title: "Sign In - CreatorCharts",
  description: "Sign in to your CreatorCharts account",
};

const SignIn = () => {
  return <SignInClient />;
};

export default SignIn;
