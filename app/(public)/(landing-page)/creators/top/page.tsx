import React from "react";
import TopCreatorClient from "./client";

export const metadata = {
  title: "Top Creators - CreatorCharts",
  description:
    "Explore the top-ranked creators on CreatorCharts. Discover leading content creators based on performance, engagement, and influence.",
};

const TopCreator = () => {
  return <TopCreatorClient />;
};

export default TopCreator;
