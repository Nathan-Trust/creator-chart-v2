import React from "react";
import ContactClient from "./client";

export const metadata = {
  title: "Contact Us - CreatorCharts",
  description:
    "Get in touch with CreatorCharts. We're here to help with questions about our data, methodology, or partnership opportunities.",
};

const Contact = () => {
  return <ContactClient />;
};

export default Contact;